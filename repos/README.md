# Git repositories to analyze
# This directory is used to store cloned repositories for analysis
# Add your repositories here, for example:
# git clone https://github.com/php/php-src.git

# Note: This directory is ignored by git (.gitignore)
# Each user should clone their own repositories to analyze
